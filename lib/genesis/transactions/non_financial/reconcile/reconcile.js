// Generated by CoffeeScript 2.5.1
(function() {
  var Base, Reconcile, config;

  Base = require('../../base');

  config = require('config');

  Reconcile = class Reconcile extends Base {
    constructor(params) {
      super(params);
      this.requiredFieldsOr = ['transaction_id', 'arn', 'unique_id'];
    }

    getUrl() {
      return {
        app: 'gate',
        path: 'reconcile',
        token: config.customer.token
      };
    }

    getTrxData() {
      return {
        'reconcile': this.params
      };
    }

  };

  module.exports = Reconcile;

}).call(this);
